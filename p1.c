#include<stdio.h>
int main()
 {
  char a[10]={'a','b','c','d','e'};
  printf("Enter the Alphabet:");
   scanf('%c',&a[i]);
   if(a[i]==a[0])
      {
      printf("The Alphet letter is:",a[i]);
      }
      else
      {
      printf("This is not Alphabet:",a[i]);
      }
      return 0;
    }  
