#include<stdio.h>
#include<conio.h>
void main()
  {
   int a;
   print("Enter the number:");
   scanf("%d",&n);
   if(a%2==0)
   {
     printf("The positive number is:%d",a);
   }
  else
  {
    printf("The negative number is:%d",a);
  }
  getch();
}
